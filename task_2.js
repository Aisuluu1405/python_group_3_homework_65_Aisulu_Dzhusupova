let firstArray = [1, 5, 12, 4, 3];
let sum = 0; 
for (let i = 0; i < firstArray.length; i++) { 
sum += firstArray[i] 
} 
let average = sum / firstArray.length; 
console.log('Среднеарифметическое значение: ' + average);

let secondArray = [2, 3, 4, 5, 6, 7, 8, 9, 10];
let sumSecond = 0; 
for (let i = 0; i < secondArray.length; i++) { 
sumSecond += secondArray[i] 
} 
let averageSecond = sumSecond / secondArray.length; 
console.log('Среднеарифметическое значение: ' + averageSecond);

let thirdArray = [3, 10, 17];
let sumThird = 0; 
for (let i = 0; i < thirdArray.length; i++) { 
sumThird += thirdArray[i] 
} 
let averageThird = sumThird / thirdArray.length; 
console.log('Среднеарифметическое значение: ' + averageThird);